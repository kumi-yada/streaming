import {Container} from '../components/Container';
import {Text} from '../components/Text';

export const EndScreen = () => {
	return (
		<Container>
			<Text pulse>Shutting down...</Text>
		</Container>
	);
};
